/**
 * content/slate-bridge.ts — Bridge into the page's MAIN world so we can drive
 * Google Flow's Slate.js editor (which is unreachable from the ISOLATED world).
 *
 * Layer: Content script (MAIN world)
 * Owner: providers/flow
 *
 * Phase 0: stub. Real implementation in Phase 2.
 * Reference: reference-ext/slate-bridge.js.
 */

console.debug('[h2-flow] slate-bridge loaded');

export {};
